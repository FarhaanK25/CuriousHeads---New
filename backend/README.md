CuriousHeads Backend
====================

Setup:
1. cd backend
2. npm install
3. copy .env.example to .env and set MONGO_URI and JWT_SECRET
4. mkdir -p uploads/notes uploads/contests
5. npm run seed
6. npm run dev

API base: http://localhost:5000/api
